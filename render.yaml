services:
  - type: web
    name: portal-bi-saas
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run mini_app.py --server.port $PORT --server.address 0.0.0.0
